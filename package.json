{
  "name": "cursor-leader",
  "author": "lumine-code",
  "description": "Keyboard-driven multi-cursor creation and navigation commands.",
  "repository": "https://github.com/lumine-code/cursor-leader",
  "bugs": {
    "url": "https://github.com/lumine-code/cursor-leader/issues"
  },
  "keywords": [
    "multi-cursor",
    "multicursor",
    "selection",
    "navigation",
    "power-mode"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "You can isolate the active cursor from all the others with {{ 'cursor-leader:power-editor' | keystroke }}",
    "You can step between your cursors with {{ 'cursor-leader:next' | keystroke }}"
  ],
  "main": "./lib/main",
  "files": [
    "keymaps",
    "lib",
    "menus",
    "spec",
    "styles"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "consumedServices": {
    "status-bar": {
      "versions": {
        "^1.0.0": "consumeStatusBar"
      }
    }
  },
  "configSchema": {
    "cursorDecoration": {
      "title": "Decoration of the last cursor",
      "description": "The main cursor is marked with special CSS when the number of cursors is more than two.",
      "type": "boolean",
      "default": true
    },
    "decorationTime": {
      "title": "Time to disappear cursor decoration",
      "description": "Set the time in ms after which decoration disappears. If the timer is set to zero, the decoration is permanent.",
      "type": "integer",
      "minimum": 0,
      "default": 2000
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  }
}
